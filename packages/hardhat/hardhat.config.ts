// Network use on deploiement
const defaultNetwork = "alpha";

const frontEndPathConfig: { [key: string]: string } = {
  react: "../react-app/src/utils/contracts",
  subgraph: "../subgraph/contracts",
};

export { frontEndPathConfig };
